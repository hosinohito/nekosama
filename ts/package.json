{
  "name": "nekosama",
  "version": "1.0.0",
  "description": "",
  "scripts": {    
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "watch 'tsc -p ./ && echo \"success\" && date' ./ -d -u",
    "which": "which watch"
  }
}